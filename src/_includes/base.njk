<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
  {%- if page.url == "/" %}
  <title>{{ site.name }} — {{ site.tagline }}</title>
  {%- else %}
  <title>{{ title }} | {{ site.name }}</title>
  {%- endif %}
  <meta name="description" content="{{ description | default(site.description) }}">
  <meta name="robots" content="index, follow">
  <link rel="icon" href="{{ '/favicon.svg' | url }}" type="image/svg+xml">
  <link rel="apple-touch-icon" href="{{ '/favicon.svg' | url }}">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
  <meta name="apple-mobile-web-app-title" content="GEO 42">
  <meta name="format-detection" content="telephone=no">
  <link rel="canonical" href="{{ site.url }}{{ page.url }}">

  {# ---- Open Graph ---- #}
  {%- if page.url == "/" %}
  <meta property="og:title" content="{{ site.name }} — {{ site.tagline }}">
  {%- else %}
  <meta property="og:title" content="{{ title }} | {{ site.name }}">
  {%- endif %}
  <meta property="og:description" content="{{ description | default(site.description) }}">
  <meta property="og:type" content="{% if page.url == '/' %}website{% else %}article{% endif %}">
  <meta property="og:url" content="{{ site.url }}{{ page.url }}">
  <meta property="og:site_name" content="{{ site.name }}">
  <meta property="og:image" content="{{ site.url }}/images/og-image.svg">
  <meta property="og:image:width" content="1200">
  <meta property="og:image:height" content="630">

  {# ---- Twitter Card ---- #}
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:image" content="{{ site.url }}/images/og-image.svg">
  {%- if page.url == "/" %}
  <meta name="twitter:title" content="{{ site.name }} — {{ site.tagline }}">
  {%- else %}
  <meta name="twitter:title" content="{{ title }} | {{ site.name }}">
  {%- endif %}
  <meta name="twitter:description" content="{{ description | default(site.description) }}">

  {# ---- Fonts (non-render-blocking preload pattern) ---- #}
  {%- if site.fonts.googleFontsUrl %}
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link rel="preload" as="style" href="{{ site.fonts.googleFontsUrl }}" onload="this.rel='stylesheet'">
  <noscript><link href="{{ site.fonts.googleFontsUrl }}" rel="stylesheet"></noscript>
  {%- endif %}
  <link rel="stylesheet" href="{{ '/css/style.css' | url }}">

  {# ---- Three.js import map (desktop only — module loaded conditionally) ---- #}
  <script type="importmap">
  {
    "imports": {
      "three": "https://cdn.jsdelivr.net/npm/three@0.169.0/build/three.module.js"
    }
  }
  </script>

  {# ---- JSON-LD ---- #}
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@graph": [
      {
        "@type": ["Organization", "ProfessionalService"],
        "@id": "{{ site.url }}/#organization",
        "name": "{{ site.name }}",
        "url": "{{ site.url }}/",
        "description": "{{ site.description }}",
        "foundingDate": "2025",
        "logo": "{{ site.url }}/favicon.svg",
        "sameAs": ["{{ site.url }}/"]
        {%- if site.contact.email %},
        "email": "{{ site.contact.email }}"
        {%- endif %}
      },
      {
        "@type": "SoftwareApplication",
        "@id": "{{ site.url }}/#software",
        "name": "{{ site.name }} Platform",
        "applicationCategory": "BusinessApplication",
        "operatingSystem": "Web",
        "description": "{{ site.description }}",
        "provider": { "@id": "{{ site.url }}/#organization" },
        "featureList": "AI Visibility Tracking, Prompt Intelligence, Citation Analytics, Crawler Analytics, Traffic Attribution, Shopping Visibility, GEO+SEO Content Marketing",
        "offers": {
          "@type": "Offer",
          "availability": "https://schema.org/OnlineOnly",
          "price": "0",
          "priceCurrency": "USD",
          "description": "Enterprise plans available — contact for pricing"
        }
      },
      {%- if page.url == "/" %}
      {
        "@type": "WebSite",
        "@id": "{{ site.url }}/#website",
        "name": "{{ site.name }}",
        "url": "{{ site.url }}/",
        "description": "{{ site.description }}",
        "publisher": { "@id": "{{ site.url }}/#organization" }
      },
      {%- endif %}
      {
        "@type": "WebPage",
        "url": "{{ site.url }}{{ page.url }}",
        "name": "{{ title | default(site.name) }}",
        "description": "{{ description | default(site.description) }}",
        "isPartOf": { "@id": "{{ site.url }}/#website" },
        "about": { "@id": "{{ site.url }}/#organization" }
      }
      {%- if page.url == "/" %},
      {
        "@type": "FAQPage",
        "mainEntity": [
          {
            "@type": "Question",
            "name": "What is Generative Engine Optimization (GEO)?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "GEO is the practice of optimizing your brand's presence in AI-generated answers. Unlike traditional SEO where you compete for page rankings, GEO is binary — you are either the cited answer or invisible. There is no page two in AI search."
            }
          },
          {
            "@type": "Question",
            "name": "How is GEO different from SEO?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "SEO is linear — sites gradually climb rankings and multiple results coexist on page one. GEO is binary — AI engines cite a handful of sources per response. You are either the definitive answer or you don't exist. GEO 42 ensures you're the answer."
            }
          },
          {
            "@type": "Question",
            "name": "What AI platforms does GEO 42 cover?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "GEO 42 monitors and optimizes brand visibility across 10+ AI platforms including ChatGPT, Google AI, Perplexity, Gemini, Claude, Copilot, DeepSeek, Doubao, Qwen, and Kimi — covering both Western and Chinese AI ecosystems."
            }
          },
          {
            "@type": "Question",
            "name": "Who should use GEO 42?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "GEO 42 is built for enterprise brands that need to maintain visibility as search shifts from traditional engines to AI-powered answers. If your customers are asking AI about your industry, you need to be the answer they receive."
            }
          },
          {
            "@type": "Question",
            "name": "How quickly can I see results?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "Most enterprise clients begin seeing measurable improvements in AI visibility within the first 30 days. Our real-time dashboard tracks citation rates, recommendation frequency, and brand sentiment across all major AI platforms from day one."
            }
          },
          {
            "@type": "Question",
            "name": "What makes GEO 42 different from traditional SEO tools?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "Traditional SEO tools track rankings on search engine results pages. GEO 42 tracks whether AI engines cite, recommend, and trust your brand. We operate in a fundamentally different space — the binary world of AI-generated answers where you're either the source or invisible."
            }
          },
          {
            "@type": "Question",
            "name": "How do I get started?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "Request a demo through our See It in Action button. Our team will walk you through the platform, show you your current AI visibility baseline, and outline a roadmap to becoming the definitive answer in your category."
            }
          }
        ]
      }
      {%- endif %}
      {%- if page.url != "/" %},
      {
        "@type": "BreadcrumbList",
        "itemListElement": [
          {
            "@type": "ListItem",
            "position": 1,
            "name": "Home",
            "item": "{{ site.url }}/"
          },
          {
            "@type": "ListItem",
            "position": 2,
            "name": "{{ title | default('Page') }}",
            "item": "{{ site.url }}{{ page.url }}"
          }
        ]
      }
      {%- endif %}
    ]
  }
  </script>
</head>
<body class="flex flex-col min-h-dvh bg-primary-900 font-sans text-body leading-relaxed text-primary-100">

  {# ---- Scroll progress bar ---- #}
  <div class="scroll-progress" aria-hidden="true"></div>

  {# ---- Cursor glow — follows pointer on desktop ---- #}
  <div class="cursor-glow" aria-hidden="true"></div>

  <a href="#main" class="skip-link">Skip to main content</a>

  {# ---- Sticky glass header ---- #}
  <header class="site-header" id="site-header">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex items-center justify-between h-16">
        <a href="{{ '/' | url }}" class="logo-mark hover:opacity-90 transition-opacity" aria-label="GEO 42 — Home">
          <span class="logo-icon" aria-hidden="true">42</span>
          <span class="logo-text"><span class="logo-geo">GEO</span><span class="logo-42"> 42</span></span>
        </a>

        <nav class="hidden lg:flex items-center gap-1" aria-label="Main navigation">
          {%- for item in nav.main -%}
          {% set itemUrl = item.url | url %}
          <a href="{{ itemUrl }}"
             class="px-3 py-1.5 text-sm font-medium rounded-md transition-colors
                    {%- if page.url == itemUrl %} text-accent-400 bg-accent-500/10{% else %} text-primary-200 hover:text-accent-400 hover:bg-primary-800{% endif %}"
             {%- if page.url == itemUrl %} aria-current="page"{% endif %}>
            {{- item.title -}}
          </a>
          {%- endfor -%}
          <a href="{{ '/contact/' | url }}"
             class="ml-3 px-5 py-2 text-sm font-bold bg-accent-500 text-white rounded-xl hover:bg-accent-400 transition-all shadow-lg shadow-accent-500/25 hover:shadow-accent-400/30 hover:scale-[1.03]">
            See It in Action
          </a>
        </nav>

        <button type="button" id="menu-toggle"
          class="lg:hidden inline-flex items-center justify-center p-2.5 min-w-[44px] min-h-[44px] rounded-md text-primary-200 hover:text-white hover:bg-primary-800 focus:outline-none focus:ring-2 focus:ring-accent-400"
          aria-expanded="false" aria-controls="mobile-nav" aria-label="Toggle navigation menu">
          <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
            <path id="menu-icon-open" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
            <path id="menu-icon-close" class="hidden" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>
    </div>
    <nav id="mobile-nav" class="hidden lg:hidden border-t border-primary-700/50" aria-label="Mobile navigation">
      <div class="max-w-6xl mx-auto px-4 py-3 space-y-1">
        {%- for item in nav.all -%}
        {% set itemUrl = item.url | url %}
        <a href="{{ itemUrl }}"
           class="block px-3 py-2 text-sm font-medium rounded-md transition-colors
                  {%- if page.url == itemUrl %} text-accent-400 bg-accent-500/10{% else %} text-primary-200 hover:text-accent-400 hover:bg-primary-800{% endif %}"
           {%- if page.url == itemUrl %} aria-current="page"{% endif %}>{{- item.title -}}</a>
        {%- endfor -%}
      </div>
    </nav>
  </header>

  <main id="main" class="flex-1 w-full{% if not fullWidth %} max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-8 sm:py-12{% endif %}">
    {{ content | safe }}
  </main>

  {# ---- Footer ---- #}
  <footer class="bg-primary-950 text-primary-100 border-t border-primary-800" aria-label="Site footer">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-12 sm:py-16">
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8">
        <div>
          <a href="{{ '/' | url }}" class="logo-mark mb-3 inline-flex" aria-label="GEO 42 — Home">
            <span class="logo-icon" aria-hidden="true">42</span>
            <span class="logo-text"><span class="logo-geo">GEO</span><span class="logo-42"> 42</span></span>
          </a>
          <p class="text-primary-400 text-sm mt-3">{{ site.tagline }}</p>
        </div>
        {%- for group in nav.footerGroups -%}
        <div>
          <p class="font-heading text-sm font-semibold text-accent-400 mb-4 uppercase tracking-wider">{{ group.heading }}</p>
          <ul class="space-y-2">
            {%- for item in group.items -%}
            {%- if item %}
            <li><a href="{{ item.url | url }}" class="text-sm text-primary-300 hover:text-white transition-colors">{{- item.title -}}</a></li>
            {%- endif -%}
            {%- endfor -%}
          </ul>
        </div>
        {%- endfor -%}
        <div>
          <p class="font-heading text-sm font-semibold text-accent-400 mb-4 uppercase tracking-wider">Contact</p>
          <address class="not-italic text-sm space-y-2 text-primary-300">
            {%- if site.contact.email %}
            <p><a href="mailto:{{ site.contact.email }}" class="hover:text-white transition-colors">{{ site.contact.email }}</a></p>
            {%- endif %}
            {%- if site.contact.url %}
            <p><a href="{{ site.contact.url }}" class="hover:text-white transition-colors" target="_blank" rel="noopener">{{ site.contact.url | replace("https://www.", "") | replace("https://", "") }}</a></p>
            {%- endif %}
          </address>
        </div>
      </div>
      <div class="accent-line mt-10 mb-6"></div>
      <p class="text-sm text-primary-500 text-center">&copy; {{ currentYear }} {{ site.name }}. All rights reserved.</p>
    </div>
  </footer>

  <script src="{{ '/js/animations.js' | url }}" defer></script>
  {# Three.js scenes — conditionally loaded on desktop pointer devices only #}
  <script type="module">
    if (window.matchMedia && window.matchMedia("(hover: hover) and (pointer: fine)").matches &&
        !(window.matchMedia("(prefers-reduced-motion: reduce)").matches)) {
      import("{{ '/js/three-hero.js' | url }}");
      import("{{ '/js/three-flywheel.js' | url }}");
      import("{{ '/js/three-cta.js' | url }}");
    }
  </script>
  <script>
    (function () {
      var toggle = document.getElementById('menu-toggle');
      var nav = document.getElementById('mobile-nav');
      var iconOpen = document.getElementById('menu-icon-open');
      var iconClose = document.getElementById('menu-icon-close');
      if (toggle && nav) {
        toggle.addEventListener('click', function () {
          var expanded = toggle.getAttribute('aria-expanded') === 'true';
          toggle.setAttribute('aria-expanded', String(!expanded));
          nav.classList.toggle('hidden');
          iconOpen.classList.toggle('hidden');
          iconClose.classList.toggle('hidden');
        });
      }
      var header = document.getElementById('site-header');
      if (header) {
        var headerScrolled = false;
        var headerRafPending = false;
        window.addEventListener('scroll', function () {
          if (!headerRafPending) {
            headerRafPending = true;
            requestAnimationFrame(function () {
              var shouldScroll = window.scrollY > 20;
              if (shouldScroll !== headerScrolled) {
                header.classList.toggle('scrolled', shouldScroll);
                headerScrolled = shouldScroll;
              }
              headerRafPending = false;
            });
          }
        }, { passive: true });
      }
    })();
  </script>
</body>
</html>
