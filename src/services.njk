---
layout: page.njk
title: Services
description: ""
---

{%- if pageContent.services.length %}
<div class="grid grid-cols-1 md:grid-cols-2 gap-8 not-prose">
  {%- for service in pageContent.services %}
  <div class="bg-primary-800 border border-primary-700 rounded-xl p-6 sm:p-8 hover:border-accent-500 transition-colors">
    <h2 class="font-heading text-xl font-semibold text-white mb-3">
      {{ service.name }}
    </h2>
    <p class="text-primary-300 text-sm leading-relaxed">
      {{ service.description }}
    </p>
  </div>
  {%- endfor %}
</div>
{%- else %}
This page will be populated with services extracted from the source PDF.

Run the pipeline to generate content:
```
npm run extract -- --pdf source-pdf/your-document.pdf
npm run merge
npm run build
```
{%- endif %}
