---
layout: page.njk
title: Services
description: ""
---

{%- if pageContent.services.length %}
<div class="grid grid-cols-1 md:grid-cols-2 gap-8 not-prose">
  {%- for service in pageContent.services %}
  <div class="bg-white border border-gray-200 rounded-lg p-6 shadow-sm">
    <h2 class="font-heading text-xl font-semibold text-primary-700 mb-3">
      {{ service.name }}
    </h2>
    <p class="text-gray-600">
      {{ service.description }}
    </p>
  </div>
  {%- endfor %}
</div>
{%- else %}
This page will be populated with services extracted from the source PDF.

Run the pipeline to generate content:
```
npm run extract -- --pdf source-pdf/your-document.pdf
npm run merge
npm run build
```
{%- endif %}
